<!DOCTYPE html>
<html lang="en">

<head>
  <title>Edit Post | MyBlog</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    body {
      background-color: #1a202c;
      background-image: radial-gradient(at 0% 0%, hsla(218, 59%, 51%, 0.15) 0, transparent 50%),
                        radial-gradient(at 100% 100%, hsla(218, 59%, 51%, 0.15) 0, transparent 50%);
    }
  </style>
</head>

<body class="text-gray-100">
  <%- include("partials/navbar") %>

  <div class="container mx-auto px-4 py-12 flex justify-center items-center">
    <div class="glass rounded-3xl shadow-2xl p-8 lg:p-12 w-full max-w-2xl" data-aos="fade-up">
      <h3 class="text-3xl md:text-4xl font-extrabold text-center mb-6 gradient-text">✏️ Edit Post</h3>
      <%- include("partials/alerts") %>
      <form action="/blogs/update/<%= post._id %>" method="POST" enctype="multipart/form-data" class="space-y-6">
        <div>
          <label for="title" class="block text-sm font-medium text-gray-300 mb-1">Title</label>
          <input type="text" id="title" name="title" value="<%= post.title %>"
            class="w-full px-4 py-3 rounded-lg glass border border-white/20 text-black placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-purple-400"
            required>
        </div>
        <div>
          <label for="content" class="block text-sm font-medium text-gray-300 mb-1">Content</label>
          <textarea id="content" name="content" rows="6"
            class="w-full px-4 py-3 rounded-lg glass border border-white/20 text-black placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-purple-400"
            required><%= post.content %></textarea>
        </div>
        <div>
          <label for="tags" class="block text-sm font-medium text-gray-300 mb-1">Tags (comma separated)</label>
          <input type="text" id="tags" name="tags" value="<%= post.tags.join(', ') %>"
            class="w-full px-4 py-3 rounded-lg glass border border-white/20 text-black placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-purple-400">
        <black
        <div>
          <label class="block text-sm font-medium text-gray-300 mb-1">Current Thumbnail</label>
          <div class="mb-4">
            <img src="/images/uploads/<%= post.thumbnail || 'default-blog.jpg' %>" alt="Current Thumbnail" class="rounded-lg w-full h-48 object-cover border border-white/20">
          </div>
          <label class="w-full text-white cursor-pointer bg-gray-700 border border-gray-600 rounded-lg focus-within:ring-2 focus-within:ring-blue-500 hover:bg-gray-600 transition-colors duration-200">
            <span class="block px-4 py-3">Choose a new file...</span>
            <input type="file" name="thumbnail" class="sr-only">
          </label>
        </div>
        <button type="submit"
          class="w-full px-4 py-3 bg-gradient-to-r from-purple-600 to-pink-600 text-black font-semibold rounded-lg shadow-lg hover:scale-105 transform transition-all duration-300">
          Update Post
        </button>
      </form>
    </div>
  </div>

  <%- include("partials/footer") %>
  <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
  <script>AOS.init({ duration: 1000, once: true });</script>
</body>

</html>