<!DOCTYPE html>
<html lang="en">

<head>
  <title>Create Post | MyBlog</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    body {
      background-color: #1a202c;
      background-image: radial-gradient(at 0% 0%, hsla(218, 59%, 51%, 0.15) 0, transparent 50%),
                        radial-gradient(at 100% 100%, hsla(218, 59%, 51%, 0.15) 0, transparent 50%);
    }
  </style>
</head>

<body class="text-gray-100">
  <%- include("partials/navbar") %>

  <div class="container mx-auto px-4 py-12 flex justify-center">
    <div class="glass rounded-3xl shadow-2xl p-8 lg:p-12 w-full max-w-2xl" data-aos="fade-up">
      <h3 class="text-3xl md:text-4xl font-extrabold text-center mb-6 gradient-text">📝 Create New Post</h3>
      <%- include("partials/alerts") %>
      <form action="/blogs/create" method="POST" enctype="multipart/form-data" class="space-y-6">
        <div>
          <label for="title" class="block text-sm font-medium text-gray-300 mb-1">Title</label>
          <input type="text" id="title" name="title"
            class="w-full px-4 py-3 rounded-lg glass border border-white/20 text-black focus:outline-none focus:ring-2 focus:ring-purple-400"
            required>
        </div>
        <div>
          <label for="content" class="block text-sm font-medium text-gray-300 mb-1">Content</label>
          <textarea id="content" name="content" rows="8"
            class="w-full px-4 py-3 rounded-lg glass border border-white/20 text-black placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-purple-400"
            required></textarea>
        </div>
        <div>
          <label for="tags" class="block text-sm font-medium text-gray-300 mb-1">Tags (comma separated)</label>
          <input type="text" id="tags" name="tags"
            class="w-full px-4 py-3 rounded-lg glass border border-white/20 text-black placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-purple-400">
        </div>
        <div>
          <label for="category" class="block text-sm font-medium text-gray-300 mb-1">Category</label>
          <select name="category" id="category"
            class="w-full px-4 py-3 rounded-lg glass border border-white/20 text-black focus:outline-none focus:ring-2 focus:ring-purple-400">
            <option value="tech">Tech</option>
            <option value="travel">Travel</option>
            <option value="food">Food</option>
            <option value="lifestyle">Lifestyle</option>
            <option value="general" selected>General</option>
          </select>
        </div>
        <div>
          <label for="thumbnail" class="block text-sm font-medium text-gray-300 mb-1">Thumbnail</label>
          <label class="w-full text-white cursor-pointer glass border border-white/20 rounded-lg focus-within:ring-2 focus-within:ring-purple-400 hover:bg-white/10 transition-colors duration-200">
            <span class=" px-4 py-3">Choose a file...</span>
            <input type="file" id="thumbnail" name="thumbnail" class="sr-only">
          </label>
        </div>
        <button type="submit"
          class="w-full px-4 py-3 bg-gradient-to-r from-purple-600 to-pink-600 text-black font-semibold rounded-lg shadow-lg hover:scale-105 transform transition-all duration-300">
          Create Post
        </button>
      </form>
    </div>
  </div>

  <%- include("partials/footer") %>

  <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
  <script>
    AOS.init({ duration: 1000, once: true });
  </script>
</body>

</html>